<?php

declare(strict_types=1);

use Illuminate\Database\Migrations\Migration;
use Illuminate\Database\Schema\Blueprint;
use Illuminate\Support\Facades\Schema;

/**
 * The overseer's running commentary on a benchmark run.
 *
 * Stored rather than streamed straight to the browser, for three reasons: the
 * commentary is generated by a MODEL and so cannot be produced inside a page
 * request without stalling the Lab's single FastCGI worker; a viewer who opens
 * the Run Room late should still see what they missed; and a run that is
 * argued about afterwards is better with the narration than without it.
 *
 * `after_activity_id` is the high-water mark the line was written from, so the
 * next batch knows where to resume and a line is never generated twice for the
 * same events.
 */
return new class extends Migration
{
    public function up(): void
    {
        Schema::create('benchmark_commentary', function (Blueprint $table): void {
            $table->id();
            $table->string('benchmark_run_id')->index();
            $table->text('line');
            $table->unsignedBigInteger('after_activity_id')->default(0);
            $table->timestamps();
        });
    }

    public function down(): void
    {
        Schema::dropIfExists('benchmark_commentary');
    }
};
